# Server port (Render / Railway / Heroku set this automatically — you can leave it blank there)
PORT=5000

# MongoDB Atlas connection string (free tier). See README for how to create it.
# Example: mongodb+srv://<dbUser>:<dbPassword>@<cluster>.mongodb.net/<dbName>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<ashif>:<28398411>@cluster0.jryyhrc.mongodb.net/?appName=Cluster0

# Password for the secret /admin-wishes page
ADMIN_PASSWORD=28398411
